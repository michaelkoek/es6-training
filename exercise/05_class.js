/**
 * CLASS - TEST
 * Theme: Pirates
 *
 *
 * 1: Create an object constructor with the pirate array (old way)
 *
 * 2: Create a pirate class
 *    - pass all the data to the pirate class
 *    - Create a new class "Ship"
 *    - Create a method inside the Ship class (the parent)
 *    - Call the parent from within the child and pass arguments
 *    - Construct a sentence with the arguments
 */

const randomPirate = ['Black Beard', 46, 'The Flying Dutchman', 'Seven Seas'];
